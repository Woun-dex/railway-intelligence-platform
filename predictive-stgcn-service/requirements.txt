# ---- Predictive STGCN service (Engine 3) — Python runtime deps ----
# Web / async runtime
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
httpx==0.27.0

# ML runtime. CPU build of PyTorch is pulled from the CPU index in the Dockerfile
# (see --extra-index-url https://download.pytorch.org/whl/cpu). PyG is optional;
# the spatial layer is implemented in pure PyTorch against the Chebyshev-scaled
# Laplacian exported by the graph engine, so torch_geometric is not required.
torch==2.3.1
numpy==1.26.4
scipy==1.13.1

# Kafka + Confluent Schema Registry (protobuf wire format, Java-interop)
confluent-kafka[protobuf,schemaregistry]==2.4.0
protobuf==4.25.3
grpcio-tools==1.62.2          # protoc for generating *_pb2 from shared-schemas

# Observability
prometheus-client==0.20.0
